version: 2

models:
  - name: mart_customers
    description: >
      A dimension table containing dates information from product tables.

    columns:
    
      - name: customer_id 
        description: Customer identifier
        tests:
          - not_null
