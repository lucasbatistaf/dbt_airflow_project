version: 2

models:
  - name: mart_orders
    description: >
      A dimension table containing dates information from product tables.

    columns:
    
      - name: order_id 
        description: Order identifier
        tests:
          - not_null
